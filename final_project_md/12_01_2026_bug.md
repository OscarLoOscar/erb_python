##### 1. 路由分流與命名空間 (URL Routing & Namespacing)
**這是導致「空白頁面」和「NoReverseMatch」的主要原因。**
檔案|Bug 前 (錯誤狀態)|Bug 後 (修復狀態)|原因說明
-|-|-|-|
config/urls.py|path('', include('products.urls', namespace='index'))|path('', include('pages.urls', namespace='pages'))|原本根目錄指向了錯誤的 App，且 Namespace 命名混亂。
config/urls.py|path('', include('pages.urls', namespace='products'))|path('products/', include('products.urls', namespace='products'))|發生 Namespace 衝突。當兩個 App 都叫 products 時，Django 會找錯地方。
products/urls.py|缺少 search 路徑|path('search/', views.search, name='search')|模板中有 {% url 'products:search' %} 但路由表沒定義。|
##### 2. 視圖邏輯與數據處理 (View Logic & Data Handling)
**解決了搜尋功能不靈敏以及數據類型不匹配的問題。**
檔案|Bug 前 (錯誤狀態)|Bug 後 (修復狀態)|原因說明
-|-|-|-|
products/views.py|只有基礎的 all() 輸出。|實作多欄位 filter() (title, price, size 等)。|讓搜尋表單真正具備過濾資料庫的功能。
products/views.py|直接傳回 request.GET。|使用 query_data = request.GET.copy() 並將 category 轉為 int。|URL 傳來的是字串，資料庫 ID 是整數，不轉換會導致 Template 判斷失效。|
##### 3. 模板語法與前端顯示 (Template Syntax & Frontend)
**解決了報錯並提升了使用者體驗。**
檔案|Bug 前 (錯誤狀態)|Bug 後 (修復狀態)|原因說明
-|-|-|-|
_search_form.html|{% if id\|stringformat:"s" == ... %}|{% if category.id == values.category %} (配合 View 的預處理)。|Django 6.0 模板引擎在 {% if %} 內使用複雜 Filter 容易報 Syntax Error。|
pages/index.html|檔案名誤拼為 indes.html。|修正為 index.html。|檔案名稱錯誤會導致 Django 找不到 Template 導致空白或報錯。|_search_form.html|硬編碼的 District 選擇。|動態循環 {% for category in categories %}。|讓搜尋選單自動同步資料庫中的分類。
##### 📂 最終修改檔案清單 (Modified Files)
1. config/urls.py：修正了全局路由的分流邏輯與命名空間。
2. pages/views.py：將 index 改回渲染正確的 template。
3. products/urls.py：補齊了 search 的路徑定義。
4. products/views.py：核心搜尋邏輯與數據清洗（Type Casting）。
5. templates/partials/_search_form.html：修正了表單提交地址、名稱與下拉選單邏輯。
   
##### 💡 經驗總結
- Namespace 是唯一身份證：在 config/urls.py 中，每個 App 應該有唯一的 namespace。
- 類型匹配 (Type Match)：在進行 == 比較前，先在 View 處理好數據類型（字串 vs 整數），不要把負擔留在 Template。
- 一步一測試：當遇到空白頁面，先用 HttpResponse("test") 確定路由有通，再回頭查 Template。